debug: true

spring:
  jpa:
    hibernate:
      ddlAuto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/pastoraldb
    username: postgres
    password: postgres

server:
  port: 8090

security:
  ignored: /**
